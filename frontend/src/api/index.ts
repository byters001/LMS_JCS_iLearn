// Axios instance, response-envelope unwrapping, and refresh-token
// interceptor go here. The only file in the app allowed to construct
// HTTP requests directly — see CLAUDE1.md "Boundary rules".
export {}
