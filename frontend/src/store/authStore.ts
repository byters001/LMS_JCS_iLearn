// Zustand store for auth/session state (access token in memory, user,
// roles, activeCollegeId). Fetched server data belongs in TanStack
// Query's cache, not here. Requires the `zustand` package to be added
// when this store is actually implemented.
export {}
